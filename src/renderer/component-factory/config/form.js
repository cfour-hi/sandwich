import { COMPONENT_TYPE } from '@/renderer/constants';

export default () => ({
  children: [COMPONENT_TYPE.手机],
  buttonProps: { text: '提交' },
  buttonStyle: {
    fontSize: '14px',
    color: '#fff',
    backgroundColor: '#409eff',
  },
  props: {},
  style: {},
});
